# 两人开发协作规则

适用范围：瑞萨与 STM32 固件、公共 C 算法、硬件接口和比赛文档。双方遵守同一流程。

## 从任务到合并

1. 先建 Issue：缺陷用 Bug，功能用 Feature，文档/工具/联调用 Task。小型拼写和维护修改可直接 PR，说明原因。
2. 指定一位 Assignee 负责实现；另一人确认功能范围、接口与验收标准。同一个任务避免两人同时改同一文件。
3. 从最新 `main` 建短期分支；尽早开 Draft PR，让另一人看到工作范围。一个 PR 只做一个主题。
4. 完成本地检查、受影响板卡的 clean build 和必要板测，填写 PR 的验证证据，再取消 Draft。
5. 手动请求另一位开发者 Review。审核人检查逻辑、接口和证据，提出修改或 Approve；机器人通过不能代替审核。
6. 所有检查通过、讨论解决、另一人批准后，由作者 Squash and merge；合并标题沿用 PR 标题。删除已合并分支。
7. `Closes #编号` 在 PR 合并到默认分支后关闭 Issue。跨多个 PR 的任务只在最终 PR 中写 Closes，其他 PR 写 Refs。

不要直接提交或推送 `main`；不要自审自合；不要在共享分支强推。紧急修复仍走 `fix/` 分支和另一人审核。比赛现场可以临时停用功能或退回已验证版本，事后记录现象与版本，不跳过修复 PR。

## 分支、PR 与提交命名

- 分支：`feat/12-encoder`、`fix/23-uart-overflow`、`docs/wiring`；自动辅助开发可用 `codex/setup-workflow`。
- 类型：`feat` 新功能、`fix` 修复、`docs` 文档、`refactor` 重构、`test` 测试、`build` 构建、`ci` 自动化、`chore` 维护。
- PR 标题：`类型(可选模块): 简述`，例如 `feat(stm32): 增加编码器采样`。模块用小写英文、数字、下划线或连字符；描述可用中文。
- 提交信息推荐同样格式；CI 强制 PR 标题与分支前缀，不强制中间提交格式。Squash 后保留清晰的主线历史。
- Dependabot PR 免除人工命名规则，仍需 CI 和人工审核，不自动合并。

```sh
git switch main
git pull --ff-only
git switch -c feat/12-encoder
# 修改、自测，然后只添加本次相关文件
git add <相关文件>
git commit -m "feat(stm32): 增加编码器采样"
git push -u origin feat/12-encoder
```

PR 落后主线时，在任务分支执行 `git fetch origin`、`git merge origin/main`，解决冲突并重新测试后推送。不要直接接受整侧冲突内容；IDE 工程文件、引脚和时钟配置冲突需要双方一起确认。新的代码提交需要重新审核。

## Issue 管理

Issue 正文写清环境、范围和可验证的完成标准。负责人在评论里更新“处理中 / 阻塞及原因 / 待验收”。使用 `P0`（失控或比赛阻断）、`P1`（关键功能阻塞）、`P2`（一般）表达优先级；标签可选，模板不依赖预建标签。

推荐按比赛阶段设置 Milestone，例如“底盘闭环”“导航联调”“赛前冻结”。重复问题链接原 Issue 后关闭；无法复现时保留版本和尝试过的场景。Feature 未达成验收不得仅因代码合并就视作完成，应开后续 Issue 并明确剩余工作。

## C 代码与工程文件

- 自研代码遵循根目录 `.clang-format`，固定使用 clang-format 18。默认 C11；首个板级工程确认真实编译器标准后同步调整 CI，不擅自假定所有瑞萨 MCU 都是 ARM。
- 第三方 SDK 放在 `vendor/` 或 `third_party/`；纯生成代码放 `generated/`。静态/格式检查还排除目录名 `Drivers`、`Middlewares`、`ra`（FSP）。这些目录仍纳入版本管理和人工审核，禁止把自研代码放进去逃避检查。
- CubeMX `Core/`、瑞萨 `src/` 可能混有用户代码，默认仍检查；优先把业务逻辑拆到独立文件。引入现成工程时单独处理格式差异，不与功能修改混成一个大 PR。
- 提交 `.ioc`、`.project`、`.cproject`、团队共享 `.settings/`、`.uvprojx`、`.uvoptx`、`.ewp`、`.eww`、链接脚本、启动文件、FSP `configuration.xml` 及必要库。配置使用相对路径，剔除个人绝对路径和凭据。
- 生成源码可以提交以便干净克隆后构建；记录生成器版本、输入配置与再生成方式。二进制 SDK 记录来源、版本与许可。
- 不提交构建目录、烧录产物、IDE 工作区缓存。共享 VS Code 配置可以提交；不要混入个人设置。

## 审核与测试底线

审核人重点看：数组边界、缓冲区长度、整数溢出、单位与符号、超时和错误处理、中断共享数据、DMA 生命周期、栈/RAM/Flash 开销。涉及电机时还要检查失联停车、限幅与急停。`volatile` 不代表原子操作或线程安全。

| 改动 | 合并前证据 |
| --- | --- |
| 仅文档或流程配置 | 文档/配置核对，CI 通过；板测写不适用及原因 |
| 公共算法 | 边界与异常输入测试；所有受影响 MCU 工程构建 |
| 驱动、引脚、时钟、中断、通信 | 对应 MCU clean build、烧录、板级日志或波形 |
| 控制、导航、运动参数 | 上述检查及必要实车验证，记录供电、场地、参数、急停结果 |

板测记录写入 PR，长期基准保存在 `docs/`：commit SHA、板卡/MCU、工具链/SDK、操作步骤、预期与实际结果、附件。涉及运动的首次测试先架空车轮，确认急停和限幅后再落地。未完成必要验证的 PR 保持 Draft。

CI 当前执行 PR 规范检查、规则脚本单元测试、受 Git 跟踪的自研 `.c/.h` 格式检查，以及 `.c` 的通用 Cppcheck 检查。无 C 文件时明确输出跳过；这不代表编译或硬件测试通过。

```sh
python -m unittest discover -s tools/ci -p "test_*.py" -v
python tools/ci/check_c.py
```

第二条需安装 `clang-format-18` 和 `cppcheck`，并先把新 C 文件加入 Git 暂存区；未跟踪文件不会扫描。使用 `clang-format-18 -i <文件>` 修复格式。Cppcheck 误报需要分析原因，只对具体位置加带说明的抑制，不整体关闭检查。

首个固件工程 PR 必须补上 MCU 完整型号、IDE/编译器/SDK 固定版本、从干净克隆构建/烧录步骤、内存预算，以及 CI 可执行的构建命令。届时接入真实 MCU 编译和纯算法主机测试；涉及专有工具链则明确许可证与 runner 方案。现阶段没有工程，不能用桌面 GCC 冒充板级编译。

## 比赛版本与回退

双方完成验收后，从 `main` 的已验证提交打 `v0.1.0` 等版本标签；Release 附固件、SHA256、对应提交、板卡/工具链信息、参数与烧录说明。赛前冻结后只接收必要修复，冻结时间在 Milestone 中约定。

回退通过新 PR 执行 `git revert <有问题的 squash 提交>`，保留历史并重新测试；不要重写 main 历史。烧录回退使用先前验证的 Release，同时确认硬件和参数兼容。
